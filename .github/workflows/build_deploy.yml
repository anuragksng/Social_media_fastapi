name: Build and Deploy code

on: [push, pull_request] 

jobs:
  job1:
    env:
      SECRET_KEY = "09d25e094faa6ca2556c818166b7a9563b93f7099f6f0f4caa6cf63b88e8d3e7"
      ALGORITHM = "HS256"
      ACCESS_TOKEN_EXPIRE_MINUTES = 700
      SQLALCHEMY_DATABASE_URL = "postgresql+psycopg://postgres:admin123@127.0.0.1:5432/postgres"
    runs-on: ubuntu-latest
    steps:
      - name: pulling git repo  # Added 'name:' and proper spacing
        uses: actions/checkout@v4  # Added missing dash and changed v7 to v4
      - name: Install python version 3.13
        uses: actions/setup-python@v6 
        with:
          python-version: '3.13' 
      - name: update pip
        run: python -m pip install --upgrade pip
      - name: install all dependencies
        run: pip install -r requirements.txt
      - name: test with pytest
        run: |
          pip install pytest
          pytest